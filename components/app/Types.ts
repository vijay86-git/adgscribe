export interface Audio {
    name: string;
    size: string;
    duration: string;
};

export interface Medicine {
    name: string;
    timing?: string;
    dosage?: string;
    duration?: string;
    frequency?: string;
};
